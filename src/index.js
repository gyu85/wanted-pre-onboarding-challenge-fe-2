/**
 * @param {string} email
 * @param {string} password
 */

function Login(email, password) {}

/**
 * @constructor
 * @param {string} token
 */

function Todo(token) {}

/**
 *
 * @param {array} lists
 */
Todo.prototype.getTodos = function (lists) {};

/**
 *
 * @param {string} todoTitle
 * @param {string} todoDetail
 */

Todo.prototype.enrollTodo = function (todoTitle, todoDetail) {};

Todo.prototype.deleteTodo = function (todoId) {};
